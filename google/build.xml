<?xml version="1.0"?>

<!-- This is the ant build file for the google caldav server module.

     Authors: Mike Douglass   douglm@rpi.edu
-->

<project name="google.compile" default="build">
  <dirname property="google.home" file="${ant.file}"/>

  <property name="source.home" location="${google.home}/src"/>
  <property name="base.name" value="google"/>

  <target name="init">
    <dirname property="this.dir" file="${ant.file}"/>
  </target>

  <!-- ========================== Base build Targets ===================
       Here we have one target building the classes and interfaces that make
       up the calendar access control suite.
       None of these targets need to be invoked explicitly though you may
       wish to during development.
       ================================================================= -->

  <target name="build" depends="init"
          description="Compile google caldav classes">
    <!-- ==================== Sources and classes ====================== -->

    <fileset id="base.java.sources" dir="${source.home}" >
      <include name="edu/rpi/**/*.java"/>
    </fileset>

    <patternset id="base.class.patternset">
      <include name="edu/rpi/**/*.class"/>
    </patternset>

    <!-- ==================== Build Target ============================= -->

    <property name="build.jar.file" location="${google.jar}" />

    <ant antfile="${buildjar}" inheritRefs="true" target="build" />
  </target>
</project>
